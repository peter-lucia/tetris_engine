[project]
name = "tetris_engine"
version = "0.3.1"
description = "A tetris engine written in rust"
license = "MIT"
readme = "README.md"


[build-system]
requires = ["setuptools", "wheel", "setuptools-rust"]
